{
  "name": "{{ project_slug }}",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@{{ tool_versions.pnpm }}",
  "engines": {
    "node": "{{ tool_versions.nodejs }}",
    "pnpm": "{{ tool_versions.pnpm }}"
  }
}
