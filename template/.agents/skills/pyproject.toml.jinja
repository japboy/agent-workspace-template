[project]
name = "{{ project_slug }}-agent-skills"
version = "0.1.0"
description = "Repository-scoped Agent Skill resources and helper scripts."
requires-python = "=={{ tool_versions.python }}"
dependencies = []

[tool.uv]
package = false
